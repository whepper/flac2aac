# Core dependencies
mutagen>=1.47.0

# Image processing (optional, for cover art)
Pillow>=10.0.0

# TOML parsing (Python < 3.11)
tomli>=2.0.0; python_version < '3.11'

# ReplayGain analysis requires rsgain system binary (not a Python package)
# Install: brew install rsgain  /  apt install rsgain
# https://github.com/complexlogic/rsgain
